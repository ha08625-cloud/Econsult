[build]
builder = "RAILPACK"
buildCommand = "pip install -r requirements.txt && cd frontend && npm install && npm run build && cd .."

[deploy]
startCommand = "PYTHONPATH=backend uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}"
