// Smoke test — verifies that the Vitest setup is working end to end.
// This file can be deleted once Phase 2 component tests are in place.

it("test infrastructure is working", () => {
  expect(1 + 1).toBe(2);
});
